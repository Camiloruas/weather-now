<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Now</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="<%= locals.weatherClass %>">
    <h1>Confira o Clima Agora!</h1>
    <form action="/weather" method="POST">
        <label for="city">Digite o nome da cidade:</label>
        <input type="text" id="city" name="city" placeholder="Ex: São Paulo">
        <button type="submit">Buscar Clima</button>
    </form>

    <% if (locals.dadosTempo) { %>
        <div id="weather-container">
            <h2>Clima em <%= dadosTempo.name %></h2>
            <p>Temperatura: <%= Math.floor(dadosTempo.main.temp - 273.15) %> °C</p>
            <p>Descrição: <%= dadosTempo.weather[0].description %></p>
        </div>
    <% } %>

    <% if (locals.errorMessage) { %>
        <div id="weather-container">
            <p style="color: red;"><%= errorMessage %></p>
        </div>
    <% } %>
    <footer id="footer">
        © 2025 Camilo Ruas. All rights reserved.
    </footer>
</body>
</html>